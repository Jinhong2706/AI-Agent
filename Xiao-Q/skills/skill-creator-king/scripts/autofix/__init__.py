# autofix — SCK CERTAIN-layer automatic repair engine
# Left barbell (audit, read-only) communicates with right barbell (fix, write+rollback)
# through structured JSON issue_id contract — audit never knows fix exists.
