---
name: {name}
description: {desc}
version: "0.1.0"
triggers: []
token_budget:
  L0_trigger: 200
  L1_core: 1100
  L2_deep: 5000
  hard_cap: 10000
template: basic
scaffold: skill-creator-king
---

# {display_name}

## Purpose

你是 [角色定位：例「Python 代码审计专家」「产品需求文档写手」]，正在为 [目标用户] 执行 [核心任务]。

> 🔧 填写提示：角色决定语气和边界。如果这是你的个人工具 → 「你正在为自己做…」；如果是通用 skill → 「你正在为用户做…」

## Context

**领域**：[属于哪个知识体系？例「Golang 后端开发」「港股打新分析」「正念修行」]

**前置知识**：[使用者需要知道什么？如果是领域新手 → 在 Instructions 里补背景；如果是专家 → 可以精简]

**数据依赖**（🔒 必须声明）：
- [ ] 纯内置知识（不需要外部数据）
- [ ] 本地文件（路径：`___`，内容说明：`___`）
- [ ] 网络搜索（搜索域：`___`，禁止搜索：`___`）
- [ ] API 接口（接口地址：`___`，认证方式：`___`）

> ⚠️ 不声明数据依赖 = validate 报 LOW 级警告。填完删掉这个提示。

## Instructions

1. **[第一步：例「接收用户输入，确认格式合法」]**
2. **[第二步：例「执行核心逻辑」]**
   - 子步骤 A
   - 子步骤 B
3. **[第三步：例「产出结果，检查异常」]**

> 🔧 填写提示：每步一个动词开头（接收/确认/执行/检查/生成/写入）。3-7 步为佳。这是 skill 的「施工图」——最不能敷衍的段落。

### 错误处理（🔒 必须填写）

| 错误场景 | 处理方式 | 降级路径 |
|----------|---------|---------|
| [例：API 超时] | [重试 2 次，间隔 3 秒] | [使用缓存数据 / 告知用户稍后重试] |
| [例：输入格式非法] | [提示用户正确格式] | [给出示例] |

> ⚠️ 不填错误处理 = 脚本失败时 AI 会乱猜原因。

## Output

**交付物类型**：[Markdown 报告 / JSON 数据 / 文件路径 / 无]

**格式模板**（用 ``` 代码块包裹）：
```
[替换为实际输出格式]
```

## Notes

- [边界条件：什么情况不该用这个 skill？]
- [已知限制：当前版本做不到什么？如实写]
- [常见陷阱：过去踩过的坑]
- **🔒 修改纪律**：本 skill 由 skill-creator-king 创建。修改后必须加载 SCK 运行 `validate.py` + `phase-check.py`，修到 0 HIGH 为止。

---

### Further Reading

- [链接到方法论文献 / 上游 skill / 外部参考资料。如无，删除此段]

_模板生成：skill-creator-king v3.18.8_
