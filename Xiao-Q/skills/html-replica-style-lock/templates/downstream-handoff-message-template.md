已输出以下文件：
- `index.html`
- `design-style-spec.md`
- `design-tokens.json`

建议将这三份文件一并交给 `frontend-skill` 或基于 `frontend-slides` 的页面生成 Skill，并明确要求：

> 请严格基于现有 HTML 基准页、风格锁定规范和设计令牌继续扩展页面。禁止更换颜色系统、字体层级、间距节奏、组件语言、卡片质感、图像裁切方式与整体页面气质。允许因内容变化做结构调整，但不得脱离当前视觉体系。
