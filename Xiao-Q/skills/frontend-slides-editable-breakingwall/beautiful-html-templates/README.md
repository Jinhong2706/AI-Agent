# beautiful-html-templates

This marketplace package includes the 34 source HTML templates used by `frontend-slides-editable` for local, reproducible template ports.

Preview screenshots are intentionally omitted from the marketplace package because binary image files are not accepted by the skill upload flow. Open the generated HTML samples under `../examples/generated/presets/` to preview the ported templates.
