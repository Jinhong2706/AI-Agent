# 读书助手 Skill 模板

> 来源：五层记忆系统v4.1.6-执行文件-WorkBuddy版.md 附录D
> 版本：v2.0（2026-04-06）
> 用途：初始化完成后，用户可将此模板安装为独立 Skill

---

## 安装说明

1. 将本文件完整内容复制到 `~/[记忆共享中心]/技能配置/读书助手/SKILL.md`
2. 在 `~/.workbuddy/skills/读书助手/` 创建软链接指向该文件
3. 重启 WorkBuddy，触发词即可生效

> **注意**：读书助手依赖3个附属文件（`references/note-template.md`、`references/quality-checklist.md`、`references/format-specs.md`）和1个配置文件（`config.yaml`），需单独准备。

---

## SKILL.md 模板内容

```markdown
---
name: 读书助手
description: 当用户需要处理书籍文件（PDF/EPUB/TXT等）并生成结构化读书笔记时触发此skill。适用于阅读书籍后提取核心观点、整理全书结构、生成笔记文档的场景。
when_to_use:
  - "用户要求处理书籍文件（PDF/EPUB/TXT等）并生成读书笔记"
  - "用户说'读这本书''帮我做笔记''提取这本书'"
  - "用户要求续写/补充/检查已有读书笔记"
  - "涉及读书笔记格式规范、质量检查、双向链接更新"
version: v2.0
last_updated: 2026-04-06
architecture: layered
---

# 读书助手

## 版本历史

| 日期 | 版本 | 变更内容 | 原因 |
|------|------|---------|------|
| 04-06 | **v2.0** | **内容分层重构**：SKILL.md从1086行瘦身至~350行 | 借鉴分层策略，降低单次加载token量 |
| 04-05 | v1.6 | 新增whenToUse声明+版本历史机制+Fail-Closed安全哲学声明 | 对标Claude Code源码分析 |
| 03-21 | v1.5 | 新增格式规范：阶段1必须读现有笔记+格式自检步骤 | 路径不一致导致写错位置 |

---

### 🔒 安全哲学：Fail-Closed 默认假设

> **核心信念**：AI会犯错。路径会变。记忆会过时。格式会漂移。
>
> 因此本Skill的**所有默认值都按"最坏情况"设计**：
> - 默认假设：路径已变更 → 必须验证（读 config.yaml）
> - 默认假设：记忆不可靠 → 必须读取实际文件
> - 默认假设：上次执行成功 → 必须重新检查
> - 默认假设：格式没漂移 → 必须对照模板

---

## ⚠️ 强制执行铁律（每次生成笔记前必须逐项确认）

### 🔒 配置验证铁律

**绝对禁止**：
- ❌ 禁止只读 skill 主文档，不读 `config.yaml`
- ❌ 禁止发现路径不一致时自由发挥判断
- ❌ 禁止未向用户确认就执行配置变更
- ❌ 禁止相信文档而不验证实际文件位置

**必须执行**：
- ✅ 必须读取 skill 主文档
- ✅ 必须读取 `config.yaml` 确认路径配置
- ✅ 必须检查现有笔记实际存放路径
- ✅ 必须对比 config.yaml 路径 vs 实际路径是否一致
- ✅ 发现不一致必须立即停止，向用户确认

---

## Overview

本skill帮助用户完整阅读书籍并生成高质量的结构化读书笔记。核心原则是**必须完整阅读书籍**，确保所有内容准确可靠。

---

## 工作流程（4阶段）

### 阶段1：读取阶段

1. **识别书籍文件格式** — PDF/EPUB/TXT/Markdown，确认路径和可访问性
2. **读取现有笔记格式（必做）** — 读至少2本现有笔记，熟悉标准格式
3. **完整阅读书籍** — 从头到尾完整读取，包括封面、目录、前言、正文、后记

### 阶段2：理解阶段

1. **把握整体框架** — 核心主题、主要论点、章节关系
2. **识别关键要素** — 重要概念、核心论据、关键案例和数据

### 阶段3：提取与整理

按9模块结构提取内容。**详细模板见 `references/note-template.md`**。

### 阶段4：质量检查

**按 `references/quality-checklist.md` 执行三步检查**。

---

## 📎 索引（上下文恢复）

| 需求 | 跳转到 |
|------|--------|
| 完整9模块模板定义 | `references/note-template.md` |
| 质量检查流程 + 防重复规范 | `references/quality-checklist.md` |
| 延伸阅读/双向链接/知识网络细节 | `references/format-specs.md` |
| 路径配置和质量阈值 | `config.yaml` |

---

*SKILL.md v2.0 — 内容分层架构。详细模板和规范已下沉到 references/ 目录。*
```
