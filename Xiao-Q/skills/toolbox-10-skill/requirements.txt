# 全能实用工具箱（SkillHub上架版）依赖包（固定安全版本，无已知漏洞）
qrcode==7.4.2          # 二维码生成，固定安全版本
pillow==10.3.0         # 图片处理（证件照、压缩、转换），最新稳定安全版本
numpy==1.26.4          # 噪音检测、数值计算，固定版本避免兼容漏洞
sounddevice==0.4.6     # 噪音检测（本地麦克风采集），稳定版本
scipy==1.11.4          # 噪音检测（分贝计算），固定安全版本
requests==2.31.0       # 第三方API调用，最新安全版本（修复请求漏洞）
json5==0.9.14          # 安全解析JSON数据，避免注入漏洞
# 可选：若SkillHub平台已预装部分依赖，可删除对应行（建议保留，确保兼容性）